# System BOOT sequence
1. Read `specs/WAL.md` to understand the current continuation state.
2. Read the specs linked in the `WAL.md` 'In Progress' section.
3. NEVER assume context from memory. ALWAYS rely on files.
4. Keep actions atomic, referencing spec URIs (e.g., `spec://whatdoesitmean/core/PROP-001#hotkey.trigger`).
5. AI MUST NOT leave overt indicators of its presence in the codebase (no "Generated by AI" comments, etc.).
